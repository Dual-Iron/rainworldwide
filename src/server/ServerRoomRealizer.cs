namespace Server;

class ServerRoomRealizer
{
    bool logged = false;
    public void Update()
    {
        if (!logged) {
            logged = true;
            Log("Server realization logic: TODO!");
        }
    }
}
